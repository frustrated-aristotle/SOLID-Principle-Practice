using System;
using UnityEngine;

public class Produce : MonoBehaviour
{
    /*
     private ProducerData _producerData;

    private void Start()
    {
        _producerData = this.GetComponent<ProducerData>();
    }

    // ReSharper disable Unity.PerformanceAnalysis
    public void StartProduce()
    {
        //Check the type of producer.
        switch (_producerData.type)
        {
            case 0:
                    Debug.Log("Produce 1");
                break;
            case 1:
                    Debug.Log("Produce 2");

                break;
            case 2:
                    Debug.Log("Produce 3");
                break;
        }
    }
    */
}
